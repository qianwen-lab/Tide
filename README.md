# Tide
A local-first, mobile-first weight journey PWA focused on trends rather than daily noise.

## GitHub Pages
Upload every file in this ZIP to the repository root (no folders required), then enable Settings → Pages → Deploy from branch → main / root.

## Privacy
Personal entries are stored in the browser's localStorage on the device. They are not committed to GitHub. Use Data → Export JSON backup regularly.
